# Files and directories to ignore during Netlify build
# This doesn't affect what gets committed to Git

# Dependencies (will be installed during build)
node_modules

# Build artifacts (will be generated during build)
.next
out
build

# Development files
*.log
.DS_Store

# Environment files (if any)
.env.local
.env.development

# IDE files
.vscode
.idea
*.swp
*.swo

# Backup files
*.backup
*.bak

# Note: data/ directory is NOT ignored - it needs to be included for API routes


