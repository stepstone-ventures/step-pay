[build]
  command = "npm run build"
  # Publish directory is handled automatically by @netlify/plugin-nextjs

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

# Redirects for client-side routing (handled by public/_redirects)
# The plugin automatically handles Next.js routing

